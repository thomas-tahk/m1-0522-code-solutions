/* exported drop */
// define a function named drop
// with arguments array and count
// use slice method with one argument count
//  assign the result to a new variable
// return the variable

function drop(array, count) {
  var droppedArray = array.slice(count);
  return droppedArray;
}

// define a function named drop
// with arguments array and count
// call slice method of array with one argument count
//  assign the result to variable droppedArray
// return droppedArray
