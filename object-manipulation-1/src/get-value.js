/* exported getValue */
// define function named getValue
// with parameters object and key
// use bracket notation to access the value of the object at key property
// return value

function getValue(object, key) {
  return object[key];
}
